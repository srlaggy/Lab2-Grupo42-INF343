syntax = "proto3";

option go_package = "lab/dataNode";

package dataNode;

message DevolverJugadas{
    string jugadas = 1;
    //No parece ser el formato adecuado
}

message solicitarJugadas{
}

service JugadasService{
    rpc SendJugadas(SolicitarJugadas) returns (DevolverJugadas);
}